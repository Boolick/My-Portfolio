import { ProjectList } from "../../shared";

export const PortfolioPage = () => {
  return <ProjectList className={"flex flex-col justify-between items-start gap-5"} />;
};

export default PortfolioPage;
